/**
 * Versión del contrato (`info.version` del OpenAPI). Se sube cuando cambia algo que un cliente
 * externo notaría: rutas, campos documentados o códigos de error.
 */
export const VERSION_CONTRATO = '1.0.0';
